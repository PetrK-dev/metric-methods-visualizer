import { BaseAlgorithm } from "./BaseAlgorithm";

/**
 * Base class for algorithms that use distance matrix as their underlying data structure.
 * Extends the BaseAlgorithm class to provide specialized functionality for working with
 * pre-computed distance matrices in metric methods like AESA and LAESA.
 */
export class DistanceMatrixAlgorithm extends BaseAlgorithm {

}